name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "0 */8 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping deployed Streamlit app
        run: curl --fail --location --max-time 30 https://mow-metrics.streamlit.app/
